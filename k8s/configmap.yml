apiVersion: v1
kind: ConfigMap
metadata:
  name: urls-config
data:
  DB_HOST: "db-service"        # must match your Postgres Service name
  DB_PORT: "5432"
  DB_NAME: "urlshortener"
  DB_USER: "postgres"
  BASE_URL: "http://localhost:8081"